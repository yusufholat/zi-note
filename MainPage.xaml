<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:helpers="clr-namespace:Zinote.Helpers"
             xmlns:models="clr-namespace:Zinote.Models"
             xmlns:local="clr-namespace:Zinote"
             x:Class="Zinote.MainPage"
             x:DataType="local:MainPage"
             Title="Zinote">

    <Shell.TitleView>
        <Grid Margin="0,0,10,0">
            <Label Text="{Binding Title}" 
                   FontSize="20" 
                   FontAttributes="Bold" 
                   VerticalOptions="Center" 
                   HorizontalOptions="Center" 
                   TextColor="{DynamicResource DynamicShellTitleColor}"/>
            <Label Text="{DynamicResource DynamicThemeIcon}" 
                   FontSize="22" 
                   VerticalOptions="Center" 
                   HorizontalOptions="End"
                   TextColor="{DynamicResource DynamicShellTitleColor}"
                   Margin="0,0,10,0">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="OnThemeClicked"/>
                </Label.GestureRecognizers>
            </Label>
        </Grid>
    </Shell.TitleView>

    <Grid RowDefinitions="Auto,Auto,*" Padding="15" RowSpacing="15" BackgroundColor="{DynamicResource DynamicPageBackground}">
        <!-- Row 0: Export -->
        <Button Grid.Row="0" Text="Export Options" Clicked="OnExportOptionsClicked" 
                BackgroundColor="{DynamicResource DynamicAccent}" 
                TextColor="{DynamicResource DynamicButtonText}" FontAttributes="Bold" CornerRadius="8" HeightRequest="36" Padding="15,0" 
                HorizontalOptions="End"/>

        <!-- Row 1: Search & Add -->
        <Grid Grid.Row="1" ColumnDefinitions="*, Auto" ColumnSpacing="10">
            <Frame Grid.Column="0" Padding="0" CornerRadius="10" HasShadow="False" BorderColor="{DynamicResource DynamicBorder}" BackgroundColor="{DynamicResource DynamicCardBackground}" HeightRequest="45">
                <SearchBar x:Name="SearchBar" Placeholder="Search terms..." TextChanged="OnSearchBarTextChanged" BackgroundColor="Transparent" VerticalOptions="Center"/>
            </Frame>
            <Button Grid.Column="1" Text="+" Clicked="OnAddClicked" FontSize="24" WidthRequest="45" HeightRequest="45" CornerRadius="22" BackgroundColor="{DynamicResource DynamicSuccessButtonBg}" TextColor="{DynamicResource DynamicButtonText}" Padding="0"/>
        </Grid>

        <!-- Row 2: List -->
        <CollectionView x:Name="ItemsCollectionView" Grid.Row="2" SelectionMode="Single" SelectionChanged="OnSelectionChanged">
            <CollectionView.ItemsLayout>
                <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
            </CollectionView.ItemsLayout>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:DictionaryItem">
                    <Frame Margin="0" Padding="0" CornerRadius="12" HasShadow="True" BorderColor="Transparent" BackgroundColor="{DynamicResource DynamicCardBackground}">
                        <Grid ColumnDefinitions="5, *, Auto">
                            <!-- Left Accent Bar -->
                            <BoxView Grid.Column="0" Color="{DynamicResource DynamicAccent}" VerticalOptions="Fill"/>
                            
                            <!-- Content -->
                            <VerticalStackLayout Grid.Column="1" Padding="15,10" Spacing="4">
                                <Label Text="{Binding SourceTerm}" FontAttributes="Bold" FontSize="18" TextColor="{DynamicResource DynamicPrimaryText}" />
                                <Label Text="{Binding TargetTerm}" FontSize="16" TextColor="{DynamicResource DynamicSecondaryText}" />
                                <Label Text="{Binding Definition}" TextColor="{DynamicResource DynamicSecondaryText}" Opacity="0.7" FontSize="12" LineBreakMode="TailTruncation" MaxLines="2" />
                            </VerticalStackLayout>
                            
                            <!-- Action -->
                             <Button Grid.Column="2" Text="✕" Clicked="OnDeleteClicked" CommandParameter="{Binding Id}" 
                                     BackgroundColor="{DynamicResource DynamicDangerBackground}" 
                                     TextColor="{DynamicResource DynamicDeleteButtonText}" FontSize="16" FontAttributes="Bold"
                                     WidthRequest="36" HeightRequest="36" CornerRadius="18" Padding="0"
                                     VerticalOptions="Center" Margin="0,0,10,0"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <!-- Use Grid.RowSpan="3" to cover the entire page content -->
        <Grid x:Name="ExportOverlay" Grid.Row="0" Grid.RowSpan="3" BackgroundColor="{DynamicResource DynamicOverlayBackground}" IsVisible="False" ZIndex="999">
            <Frame BackgroundColor="{DynamicResource DynamicCardBackground}" CornerRadius="10" HorizontalOptions="Center" VerticalOptions="Center" Padding="25" WidthRequest="320" BorderColor="Transparent">
                <VerticalStackLayout Spacing="15">
                    <Label Text="Export Options" FontSize="20" FontAttributes="Bold" HorizontalOptions="Center" TextColor="{DynamicResource DynamicPrimaryText}" Margin="0,0,0,10"/>

                    <!-- Basic -->
                    <Label Text="Basic Data" FontSize="12" TextColor="{DynamicResource DynamicSecondaryText}" FontAttributes="Bold"/>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Grid.Column="0" Text="CSV" Clicked="OnExportFormatClicked" CommandParameter="Basic CSV" BackgroundColor="{DynamicResource DynamicExportBasicBg}" TextColor="{DynamicResource DynamicExportBasicText}" BorderColor="{DynamicResource DynamicBorder}" BorderWidth="1"/>
                        <Button Grid.Column="1" Text="Excel" Clicked="OnExportFormatClicked" CommandParameter="Basic Excel" BackgroundColor="{DynamicResource DynamicExportBasicBg}" TextColor="{DynamicResource DynamicExportBasicText}" BorderColor="{DynamicResource DynamicBorder}" BorderWidth="1"/>
                    </Grid>

                    <!-- Matecat -->
                    <Label Text="Matecat" FontSize="12" TextColor="{DynamicResource DynamicSecondaryText}" FontAttributes="Bold" Margin="0,10,0,0"/>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Grid.Column="0" Text="CSV" Clicked="OnExportFormatClicked" CommandParameter="Matecat CSV" BackgroundColor="{DynamicResource DynamicExportMatecatBg}" TextColor="{DynamicResource DynamicExportMatecatText}" BorderColor="{DynamicResource DynamicExportMatecatText}" BorderWidth="1"/>
                        <Button Grid.Column="1" Text="Excel" Clicked="OnExportFormatClicked" CommandParameter="Matecat Excel" BackgroundColor="{DynamicResource DynamicExportMatecatBg}" TextColor="{DynamicResource DynamicExportMatecatText}" BorderColor="{DynamicResource DynamicExportMatecatText}" BorderWidth="1"/>
                    </Grid>

                    <!-- Smartcat -->
                    <Label Text="Smartcat" FontSize="12" TextColor="{DynamicResource DynamicSecondaryText}" FontAttributes="Bold" Margin="0,10,0,0"/>
                    <Grid ColumnDefinitions="*,*" ColumnSpacing="10">
                        <Button Grid.Column="0" Text="CSV" Clicked="OnExportFormatClicked" CommandParameter="Smartcat CSV" BackgroundColor="{DynamicResource DynamicExportSmartcatBg}" TextColor="{DynamicResource DynamicExportSmartcatText}" BorderColor="{DynamicResource DynamicExportSmartcatText}" BorderWidth="1"/>
                        <Button Grid.Column="1" Text="Excel" Clicked="OnExportFormatClicked" CommandParameter="Smartcat Excel" BackgroundColor="{DynamicResource DynamicExportSmartcatBg}" TextColor="{DynamicResource DynamicExportSmartcatText}" BorderColor="{DynamicResource DynamicExportSmartcatText}" BorderWidth="1"/>
                    </Grid>

                    <Button Text="Close" Clicked="OnExportOverlayClose" BackgroundColor="{DynamicResource DynamicCloseButtonBg}" TextColor="{DynamicResource DynamicButtonText}" Margin="0,20,0,0"/>
                </VerticalStackLayout>
            </Frame>
        </Grid>
    </Grid>

</ContentPage>
